server:
  port: 8079
spring:
  datasource:
    username: postgres
    password: 123
    url: jdbc:postgresql://localhost:5432/revolusion_db
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true